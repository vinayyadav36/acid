# =============================================================================
# ACID - Python dependencies
# =============================================================================
# Install with:
#   pip install -r requirements.txt
#
# Used by:
#   scripts/analytics.py  — offline analytics / report helper
# =============================================================================

# Database driver for PostgreSQL (binary wheel — no compiler needed)
psycopg2-binary>=2.9

# Load .env files into os.environ
python-dotenv>=1.0
